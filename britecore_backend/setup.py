# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name='britecore',
    version='1.0.0',
    description='Engineering Project',
    author='Ariel Henrique Santos Neves',
    author_email='ariel@gmail.com',
    packages=find_packages(exclude=('tests', 'docs'))
)
